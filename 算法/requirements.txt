# 基础库
numpy==1.26.0
opencv-python==4.8.0.76  # cv2的依赖
scikit-learn==1.3.0      # SVM、特征预处理等
scikit-image==0.22.0     # GLCM特征提取（skimage.feature）
python-multipart==0.0.6  # 处理文件上传（若用FastAPI）

# 深度学习库（刘长儒2024文献用到YOLOv8）
torch==2.0.1
#ultralytics==8.0.196     # YOLOv8的Python库

# 接口服务（供后端调用）
fastapi==0.103.1
uvicorn==0.23.2          # FastAPI的服务器